#!/usr/bin/env bash
# ============================================================================
# Fedora 42 + KDE Plasma 6 — macOS-style setup
# Run as normal user after first boot.
# Usage: bash postinstall.sh [--all | --repos | --fonts | --apps | --themes |
#          --kvantum | --icons | --decorations | --wallpapers | --panel | --konsole]
#
# TODO (Slice 2): implement all modules. This file is a scaffold stub only.
# ============================================================================
set -euo pipefail

echo "Fedora postinstall stub — not yet implemented. See Slice 2." >&2
exit 1
